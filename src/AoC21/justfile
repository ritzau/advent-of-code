# Advent of Code 2021 Solutions

# Build the project
build:
    nix build

# Run day 1
run:
    nix run

# Format code
format:
    ormolu -i *.hs

# Lint code
lint:
    hlint .

# Clean build artifacts
clean:
    rm -rf dist-newstyle/ result .ghc.environment.*
