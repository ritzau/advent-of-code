import { describe, test } from "node:test";
import assert from "node:assert";
import { solvePart1, solvePart2 } from "./common.js";

const sampleInput = ``;

describe("Part 1", () => {
  test("should solve sample input correctly", () => {
    const result = solvePart1(sampleInput);
    assert.strictEqual(result, 0); // Update with expected value
  });
});

describe("Part 2", () => {
  test("should solve sample input correctly", () => {
    const result = solvePart2(sampleInput);
    assert.strictEqual(result, 0); // Update with expected value when implemented
  });
});
